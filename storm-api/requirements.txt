# Vercel Python Serverless Requirements - Minimal Test
# Testing which packages cause failures

# Core Framework only
fastapi>=0.104.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
httpx>=0.25.0

# Rate limiting
slowapi>=0.1.9
limits>=3.0.0

# Redis (for direct Redis connections)
redis>=5.0.0

# PostgreSQL (for permanent storage)
asyncpg>=0.29.0
