# STORM Frontend Environment Variables
# Copy this to .env.local for local development

# =============================================================================
# Backend API Configuration (Required for Production)
# =============================================================================

# URL of the STORM API backend (MUST use NEXT_PUBLIC_ prefix for client-side access)
# For local development: http://localhost:8000
# For production: Your deployed backend URL (e.g., https://storm-api-xxx.vercel.app)
#
# Set this in Vercel Dashboard > Your Frontend Project > Settings > Environment Variables
NEXT_PUBLIC_STORM_API_URL=http://localhost:8000

# =============================================================================
# Optional: Analytics & Monitoring
# =============================================================================

# Vercel Analytics (automatically configured on Vercel)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# =============================================================================
# Development Settings
# =============================================================================

# Enable debug mode for more verbose logging
# NEXT_PUBLIC_DEBUG=false
